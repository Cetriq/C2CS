# Figures

Figures for the C2CS whitepaper.

Place source-editable figures (e.g. `.drawio`, `.svg`) alongside exported raster/vector
versions (`.png`, `.pdf`) referenced from `../c2cs-whitepaper.md`. Name files after the
section or concept they illustrate (e.g. `pipeline-code-to-policy.svg`).
